{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "buildCommand": "apt-get update && apt-get install -y unixodbc-dev && npm install",
    "installCommand": "apt-get update && apt-get install -y unixodbc-dev && npm install"
}